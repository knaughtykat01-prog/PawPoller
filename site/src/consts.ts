// Single source of truth for the marketing site's advertised version.
// Keep in step with config.py:APP_VERSION on each release (the /pp-release
// flow bumps that; mirror it here so the download CTA never goes stale).
export const SITE_VERSION = '2.144.0';
